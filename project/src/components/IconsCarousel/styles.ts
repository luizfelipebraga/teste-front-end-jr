import styled from 'styled-components';

export const Container = styled.div`
  margin-top: 3rem;
  max-width: 80rem;
  margin-inline: auto;

  display: flex;
  justify-content: space-between;
  align-items: center;
`;